import { MovieSummary } from '../models/MovieSummary';
import './MovieCardGrid.scss';

function MovieCardGrid({movies}: {movies: MovieSummary[]}){
    return <div>Movie Card Grid</div>
}

export default MovieCardGrid;